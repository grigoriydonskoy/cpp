#include <iostream>
using namespace std;

double squared_sum(double a, double b) {
    return (a + b) * (a + b);
}

int main() {
    double a, b;
    
    cout << "Введите два числа: ";
    cin >> a >> b;
    
    cout << "Квадрат суммы = " << squared_sum(a, b) << endl;
    
    return 0;
}
